services:
  - type: web
    name: multi-shop-django
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn MultiShop.wsgi:application --bind 0.0.0.0:$PORT"
